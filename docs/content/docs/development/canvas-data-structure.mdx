---
title: 画布数据结构
description: 画布项目存储、节点结构、媒体文件与清理机制
---

# 画布数据结构

本文档说明当前画布的服务端存储结构、前端加载方式、媒体文件和清理机制。

## 当前存储位置

- 每个画布项目独立保存在 Postgres `user_canvas_projects`，列表接口只读取标题、时间和节点/连线数量，详情接口按项目 ID 读取 `data_json`。
- 图片、视频和音频二进制保存在 `user_files.content`，通过 `storageKey` 与画布节点关联。
- `user_resources` 保存统一资源索引；媒体节点只保存 `storageKey` 和元数据，文本节点继续保存内容副本。
- 画布 JSON 不长期保存 `blob:` URL、文件接口 URL或带 `storageKey` 的 base64；自动保存前会清空这些临时字段。
- 浏览器旧版 localForage 数据只用于首次登录迁移，不再作为画布日常持久化来源。

## 画布项目结构

每个画布项目是一个 `CanvasProject`：

```ts
type CanvasProject = {
  id: string;
  title: string;
  createdAt: string;
  updatedAt: string;
  nodes: CanvasNodeData[];
  connections: CanvasConnection[];
  chatSessions: CanvasAssistantSession[];
  activeChatId: string | null;
  backgroundMode: "lines" | "dots" | "blank";
  viewport: { x: number; y: number; k: number };
};
```

字段说明：

- `id`：画布项目 ID，当前前端生成。
- `title`：画布名称。
- `createdAt` / `updatedAt`：ISO 字符串。
- `nodes`：画布节点列表。
- `connections`：节点连线列表。
- `chatSessions`：右侧画布助手会话。
- `activeChatId`：当前选中的助手会话 ID。
- `backgroundMode`：画布背景模式。
- `viewport`：视口变换，`x/y` 是屏幕平移，`k` 是缩放比例。

## 节点结构

每个节点是一个 `CanvasNodeData`：

```ts
type CanvasNodeData = {
  id: string;
  type: "image" | "text" | "config" | "video" | "audio";
  title: string;
  position: { x: number; y: number };
  width: number;
  height: number;
  metadata?: CanvasNodeMetadata;
};
```

通用字段：

- `id`：节点 ID。
- `type`：节点类型，当前有图片、文本、生成配置、视频、音频五类。
- `title`：节点标题。
- `position`：画布世界坐标，不是屏幕坐标。
- `width` / `height`：画布世界坐标下的节点尺寸。
- `metadata`：节点内容和业务状态。

`metadata` 当前常用字段：

```ts
type CanvasNodeMetadata = {
  content?: string;
  composerContent?: string;
  resolvedPrompt?: string;
  prompt?: string;
  status?: "idle" | "success" | "loading" | "error";
  errorDetails?: string;
  fontSize?: number;
  generationMode?: "text" | "image" | "video" | "audio";
  model?: string;
  size?: string;
  count?: number;
  naturalWidth?: number;
  naturalHeight?: number;
  freeResize?: boolean;
  isBatchRoot?: boolean;
  batchRootId?: string;
  batchChildIds?: string[];
  primaryImageId?: string;
  imageBatchExpanded?: boolean;
  inputOrder?: string[];
  storageKey?: string;
  mimeType?: string;
  bytes?: number;
};
```

不同节点的使用方式：

- 图片节点：`storageKey` 是长期文件标识；加载详情后，前端同步把 `content` 派生为鉴权文件 URL。
- 视频节点：使用相同的文件 URL，并通过 HTTP Range 按分片读取；`bytes/mimeType/durationMs` 保存文件信息。
- 音频节点：使用鉴权文件 URL 和 metadata 预加载；`bytes/mimeType/durationMs` 保存文件信息。
- 文本节点：`content` 保存文本内容；`fontSize` 保存字体大小；`prompt/status/errorDetails` 保存生成状态。
- 生成配置节点：`generationMode/model/size/count/inputOrder` 保存生成配置；`generationMode` 可选择文本、图片或视频；上游输入通过 `connections` 计算。
- 生成配置节点的 `composerContent` 保存含 `@[node:id]` 的用户模板，`resolvedPrompt` 仅记录最近一次动态解析结果；生成不能把解析结果回写到模板。
- 图片组节点：根节点用 `isBatchRoot/batchChildIds/primaryImageId/imageBatchExpanded` 记录批量生成结果；子图节点用 `batchRootId` 指回根节点。

## 连线结构

每条连线是一个 `CanvasConnection`：

```ts
type CanvasConnection = {
  id: string;
  fromNodeId: string;
  toNodeId: string;
};
```

连线只保存节点 ID，不保存端口坐标。渲染时根据节点位置和尺寸计算路径。

删除节点时会同步删除以该节点为起点或终点的连线。删除图片组根节点时，会把对应子节点一起删除。

## 助手会话结构

助手会话保存在画布项目内：

```ts
type CanvasAssistantSession = {
  id: string;
  title: string;
  messages: CanvasAssistantMessage[];
  createdAt: string;
  updatedAt: string;
};
```

消息结构：

```ts
type CanvasAssistantMessage = {
  id: string;
  role: "user" | "assistant";
  mode: "ask" | "image";
  text: string;
  isLoading?: boolean;
  references?: CanvasAssistantReference[];
  images?: CanvasAssistantImage[];
};
```

图片引用和助手生成图片也遵循同一套图片存储规则：

- `storageKey` 存在时，以它派生文件 URL，不在进入画布时预下载附件。
- 发送到 AI 接口前，如果接口需要 base64，再通过 `imageToDataUrl` 按需读取并转换。

## 图片写入流程

所有新增图片应通过 `uploadImage(input)` 写入：

1. 传入 `Blob` 或 data URL。
2. 内部转成 `Blob`。
3. 生成 `storageKey`，格式为 `image:<id>`。
4. 把 Blob 写入 `user_files`。
5. 创建 `blob:` URL，并缓存在内存 `objectUrls`。
6. 读取图片宽高，返回：

```ts
type UploadedImage = {
  url: string;
  storageKey: string;
  width: number;
  height: number;
  bytes: number;
  mimeType: string;
};
```

图片节点会通过 `imageMetadata(image)` 写入：

```ts
{
  content: image.url,
  storageKey: image.storageKey,
  status: "success",
  naturalWidth: image.width,
  naturalHeight: image.height,
  bytes: image.bytes,
  mimeType: image.mimeType
}
```

因此，`content` 只适合当前浏览器会话展示，不能作为长期文件标识；长期标识是 `storageKey`。

## 图片异步生成

- OpenAI 格式的普通生图和图片编辑默认携带 `async=true`；Gemini `generateContent` 请求体默认携带 `async: true`。
- 创建接口直接返回图片时立即沿用同步流程；返回任务 ID 时轮询同一渠道的 `/v1/images/tasks/:task_id`。
- 任务查询兼容 OpenAI 图片数组和 Gemini `candidates` 图片结果，沿用当前请求的鉴权和取消信号。
- 只有首次创建明确不兼容 `async` 时才去掉参数同步重试；任务 ID 一旦产生，查询失败不会创建第二个生成请求。
- 不支持异步的渠道、接口和模型组合只在当前浏览器会话中缓存，刷新后重新探测。

## 画布和媒体读取

1. `GET /api/user/projects` 只返回 `CanvasProjectSummary[]`。
2. 进入画布后通过 `GET /api/user/projects/[id]` 读取当前项目详情，并立即渲染节点框架。
3. 节点根据 `storageKey` 同步生成 `/api/storage/files/[key]`，不等待媒体下载完成。
4. 画布只渲染视口及周围预加载区域的节点，因此视口外媒体不会发起请求。
5. 图片使用浏览器懒加载；视频和音频使用 `preload="metadata"`，视频开放式 Range 每次最多读取 4 MiB。

## 资源删除和清理

- 删除节点、连线、画布、生成记录、素材条目或助手会话都不会物理删除资源。
- 从“我的素材”移除只取消资源的 `is_saved`，文件和资源索引继续保留。
- 资源只能由管理员在 `/admin/resources` 手动强制删除、清空，或由自动保留任务删除。
- 管理员删除媒体资源时会删除原文件和 `preview:image:...` 预览缓存，但保留画布、节点、生成记录和素材条目；对应图片、视频、音频位置显示“资源已删除”。
- 管理员删除文本资源不会修改已有文本节点，节点继续显示自身保存的文本副本。
- 自动清理按资源 `created_at` 和类型保留天数判断，跳过已存入素材的资源，但不保护画布引用。
